<Window x:Class="BAT_Man.Views.EditFirmaDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        
        Title="Firma bearbeiten" 
        Height="650" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackground}"
        
        SizeToContent="Height">
    <!-- 'SizeToContent="Height"' bewirkt, dass sich die Fensterhöhe automatisch dem Inhalt anpasst. -->

    <!-- 
        INHALT:
        Das Fenster fungiert lediglich als Rahmen.
        Der Inhalt wird über das Binding gesetzt ({Binding}). 
        Da der DataContext im aufrufenden ViewModel auf 'FirmaAnlegenViewModel' gesetzt wird,
        sucht WPF automatisch nach dem passenden DataTemplate und zeigt die 'FirmaAnlegenView' an.
    -->
    <ContentControl Content="{Binding}" />

</Window>
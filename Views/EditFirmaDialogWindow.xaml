<Window x:Class="WPF_Test.Views.EditFirmaDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        
        Title="Firma bearbeiten" 
        Height="650" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackground}"
        
        SizeToContent="Height">
    <!--  SizeToContent sorgt dafür, dass sich das Fenster der Höhe des Inhalts anpasst -->

    <!--  
        INHALT:
        Das Fenster ist nur ein Container.
        Der Inhalt wird über Binding gesetzt. Da wir im C#-Code (FirmaAnzeigenViewModel) schreiben:
        "dialog.DataContext = viewModel", wird hier automatisch die passende View angezeigt,
        sofern ein DataTemplate in der App.xaml existiert.
        
        Alternativ (einfacher für Azubis): 
        Wir könnten hier direkt
    <local:FirmaAnlegenView /> einbinden, wenn wir sicher sind, 
        dass dieses Fenster NUR dafür genutzt wird.
    -->
    <ContentControl Content="{Binding}" />

</Window>
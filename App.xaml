<Application x:Class="BAT_Man.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BAT_Man"
             xmlns:viewmodels="clr-namespace:BAT_Man.ViewModels"
             xmlns:views="clr-namespace:BAT_Man.Views"
             xmlns:converters="clr-namespace:BAT_Man.Converters"
             ShutdownMode="OnExplicitShutdown">

    <!-- 
        ShutdownMode: "OnExplicitShutdown".
        Verhindert das automatische Beenden der App beim Wechsel vom Login- zum Hauptfenster.
    -->
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

                <!-- 
                  1. Globale Ressourcen laden:
                     Diese Resourcen tragen zur besseren Wartbarkeit bei.
                     Hier werden externe Dateien eingebunden, damit die App "weiß", wie Buttons aussehen (BasisStyles) und welche Sprache aktuell ist.
                -->
                <ResourceDictionary Source="/Styles/BasisStyles.xaml"/>
                <ResourceDictionary Source="/Themes/Dark.xaml"/>
                <ResourceDictionary Source="/Languages/German.xaml"/>
                <ResourceDictionary Source="/Help/HelpGerman.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 2. Globaler Converter für Menü-Logik (View-Erkennung) -->

            <converters:InstanceToTypeConverter x:Key="InstanceToTypeConverter"/>
            <converters:MultiParameterConverter x:Key="MultiParameterConverter"/>

            <!-- 
                3. MVVM Navigation (DataTemplates) Dies ist die "Landkarte" für WPF:
                "Wenn du im Code auf ein ViewModel triffst, zeige dem User das passende Bild (View)."
            -->

            <DataTemplate DataType="{x:Type viewmodels:WelcomeViewModel}">
                <views:WelcomeView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:FirmenUebersichtViewModel}">
                <views:FirmenUebersichtView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:FirmaAnlegenViewModel}">
                <views:FirmaAnlegenView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:FirmaAnzeigenViewModel}">
                <views:FirmaAnzeigenView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:LoginViewModel}">
                <views:LoginWindow/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:HilfeViewModel}">
                <views:HilfeView/>
            </DataTemplate>

        </ResourceDictionary>
    </Application.Resources>
</Application>